spring:
  datasource:
    username: postgres
    password: 0
    url: jdbc:postgresql://localhost:5432/todo
    hikari:
      schema: todo
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
    database: postgresql

